trust-policy = off
# The `packageManager` field above is pinned to aube for local/CI use.
# Cloudflare Pages invokes pnpm directly (aube isn't available there), so
# disable pnpm's strict packageManager-mismatch check (pnpm <11).
package-manager-strict = false
